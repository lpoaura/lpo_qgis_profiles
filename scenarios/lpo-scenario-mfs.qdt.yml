# yaml-language-server: $schema=https://raw.githubusercontent.com/Guts/qgis-deployment-cli/main/docs/schemas/scenario/schema.json

metadata:
  title: "Scenario installation Annuaire MFS"
  id: qdt-lpo-mfs
  description: >-
    LPO instllation annuaire de compétences.

# Toolbelt settings
settings:
  # QGIS_EXE_PATH:
  #   linux: /usr/bin/qgis
  #   mac: /usr/bin/qgis
  #   windows: "%PROGRAMFILES%/QGIS/3_28/bin/qgis-ltr-bin.exe"
  SCENARIO_VALIDATION: true

# Deployment workflow, step by step
steps:
  - name: Variables d'environnement persistantes
    uses: manage-env-vars
    with:
      - name: PGSERVICEFILE
        value: $USERPROFILE/pg_service_mfs.conf
        value_type: path
        scope: "user"
        action: "add"

  - name: Download profiles from remote git repository
    uses: qprofiles-downloader
    with:
      source: https://github.com/lpoaura/lpo_qgis_profiles.git
      protocol: git_remote
      branch: main

  - name: Download profiles from remote git repository
    uses: qprofiles-synchronizer
    with:
      sync_mode: overwrite

  - name: Download plugins
    uses: qplugins-downloader
    with:
      force: false
      threads: 5

  - name: Synchronize plugins
    uses: qplugins-synchronizer
    with:
      action: create_or_restore

  - name: Create shortcuts for profiles
    uses: shortcuts-manager
    with:
      action: create_or_restore
      include:
        - profile: mfs
          label: "Annuaire MFS"
          desktop: true
          start_menu: true
          icon: "mfs.ico"

  - name: Set splash screen
    uses: splash-screen-manager
    with:
      action: create_or_restore
      strict: false
